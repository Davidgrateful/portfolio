STRICT RULE — READ BEFORE DOING ANYTHING:

Do NOT modify, refactor, or redesign existing code unless I explicitly ask you to.

Specifically:
- Preserve all existing component structure, file layout, and folder organization
- Preserve all existing class names, IDs, and styling conventions
- Preserve all existing color schemes, typography, spacing, and UI patterns
- Preserve all existing logic, function names, and variable naming conventions
- Do NOT auto-fix code style, reformat files, or "clean up" anything I didn't ask about

When I ask you to add a feature or fix something:
- Add ONLY what was asked — nothing more
- Match the existing code style exactly
- If the task requires a new component or file, follow the same structure and patterns already in the project
- If you are unsure what pattern is being used, ASK me — don't assume and diverge

The existing codebase IS the design system. Treat it as the source of truth.
Deviation from it without explicit instruction is an error, not an improvement.